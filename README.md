# Ick List

iOS app plus Python backend for Instagram graph and semantic search.

## Structure

- `InstaSemanticSearch/`: Swift iOS app
- `backend/`: FastAPI backend

## Notes

- The backend is deployed from the `backend/` root directory on Render.
- The iOS app points to the backend base URL from its central config.
